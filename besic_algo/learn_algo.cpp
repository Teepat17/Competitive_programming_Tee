#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    char s= 's';
    if(islower(s)){
        cout << s;
    } else cout << (char)tolower(s);

    return 0;
}